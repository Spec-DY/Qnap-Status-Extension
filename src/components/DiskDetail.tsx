import React from "react";
import { Container, Typography } from "@mui/material";

const DiskDetail: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4">Disk Details</Typography>
      {/* 在这里添加磁盘详情的内容 */}
    </Container>
  );
};

export default DiskDetail;
